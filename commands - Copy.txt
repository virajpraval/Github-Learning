$ git config --global user.email "er.virajraval@gmail.com"
$ git config --global user.name "virajpraval"

$ git init

git status

$ git add -A

$ git commit -m "code1.txt"  == GIT Ready and green check box

git remote add origin https://github.com/virajpraval/Github-Learning.git  == Link with GITHUB 

git push -u origin master  == push code to GIT 

How to pull code ?
1. create folder
2. Go to this folder in GIT bash 
3. $ git init
4. $ git remote add origin https://github.com/virajpraval/Github-Learning.git
5. $ git pull origin master

After pull made changes to file and want to commit ??
6. made code changes --> Save -> Green will be red now
7. open GIt bash and Run below
8.  $ git status
9.  $ git add -A
10. $ git commit -m "commands.txt"
11. $ git push -u origin master

How to check list of branches ?
$ git branch

How to create branch ??
$ git branch viraj-prod
